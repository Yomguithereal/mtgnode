(function(undefined) {
  'use strict';

  /**
   * Playground Namespace
   * =====================
   *
   * Register some global configuration.
   */
  var _config = {};

  /**
   * Exporting
   * ----------
   */
  utilities.pkg('playground.config', _config);
}).call(this);

/**
 * MTGNode Alert Panel
 * ====================
 *
 * A component waiting for alert events to be dispatched and displaying
 * feeback to inform the user.
 */
var React = require('react'),
    controller = require('../../controllers/main.js');
